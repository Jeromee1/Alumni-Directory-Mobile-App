package com.apa.alumnidirectory.data.model.ui

data class DashboardUiState(
    val appealCount: Int = 0,
    val pendingCount: Int = 0,
    val approvedCount: Int = 0,
    val recentApprovedCount: Int = 0
)